name: Route Shell Smoke

on:
  pull_request:
    paths:
      - 'app/**'
      - 'components/**'
      # Layer-2's whole point is catching runtime regressions in the
      # redirect path. Anything the redirect or shell touches at boot
      # belongs here — store, persistence, schema, and the helpers under
      # lib/routes/. Don't narrow this without a Layer-2 alternative for
      # the dropped paths; the previous filter (lib/routes/** only) let
      # store/persistence changes silently bypass the gate.
      - 'lib/**'
      - 'tests/e2e/**'
      - 'playwright.config.ts'
      - 'package.json'
      - 'pnpm-lock.yaml'
      - 'bun.lock'
      - '.github/workflows/route-shell.yml'
  push:
    branches: [main]
    paths:
      - 'app/**'
      - 'components/**'
      - 'lib/**'
      - 'tests/e2e/**'
      - 'playwright.config.ts'

concurrency:
  group: route-shell-${{ github.ref }}
  cancel-in-progress: true

jobs:
  route-shell:
    name: Smoke
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Install Playwright Chromium
        run: bunx playwright install --with-deps chromium

      - name: Build Next.js
        run: bun run build

      - name: Run route-shell smoke
        run: bunx playwright test

      - name: Upload Playwright report on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 7
